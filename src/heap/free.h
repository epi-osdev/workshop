#ifndef __EOS_HEAP_FREE_H__
    #define __EOS_HEAP_FREE_H__

/**
* @brief This function free the buffer in the heap
* @param ptr The address of the buffer to free
*/
void heap_free(void *ptr);

#endif