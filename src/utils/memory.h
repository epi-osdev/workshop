#ifndef __EOS_UTILS_MEMORY_H__
    #define __EOS_UTILS_MEMORY_H__

/**
 * @brief      This is the global API for the memory utils.
 * all the functions that could be called by other files must be included here.
 * And this file must be the only one to be included when using the memory utils.
*/

    #include "memory/memset.h"

#endif
